# Sequences

## What?

This is a generator for different types of sequences.

## Usage

```
>>> import sequences
>>> linearSequence = sequences.sequence(term = 0,
 nextTermFunction = sequences.nextFunctions.linear(3))
```